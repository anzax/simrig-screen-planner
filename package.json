{
  "name": "simrig-hub",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter screen-planner dev",
    "build": "pnpm --filter screen-planner build",
    "test": "pnpm --filter screen-planner test",
    "lint": "pnpm --filter screen-planner lint",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "vitest": "^3.1.2",
    "eslint": "^9.22.0"
  }
}
